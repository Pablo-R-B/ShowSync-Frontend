export interface GeneroMusical {
  id: number;
  nombre: string;
  // No incluir las relaciones ManyToMany a menos que las necesites en el frontend
  // artistas?: Artista[];
  // eventos?: Evento[];
}


